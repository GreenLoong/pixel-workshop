# Qt 6 + OpenCV 4 批量图像处理工具 · 开发文档

- 创建日期：2026-09-23
- 最后更新：2026-09-26
- 文档版本：0.2
- 当前阶段：环境配置（OpenCV 运行验证待完成）
- 项目状态：Qt 窗口已运行，OpenCV 已完成 CMake 查找与链接，运行验收待完成

## 1. 第一目标：从学生思维转向工程师思维

我已经学习 C++ 和 Qt 6 的基本操作，也跟着完成过项目。目前，我还缺少从需求分析到最终交付的独立开发经历。我计划通过本项目完成需求拆解、技术选型、编码、排错、验证与发布，并记录实现过程和技术决策。

我的开发工作流程：

**面对模糊化的问题 → 拆解分析 → 找到根因 → 对比方案 → 落地实现 → 验证结果与复盘。**

面对需求，我先确认使用场景、具体困难和预期结果；面对故障，我通过复现、日志和对照实验定位原因。暂时无法确定时，我会将原因标记为待验证假设，并记录验证方法。

| 环节 | 要回答的问题 | 需要留下的产出 |
| --- | --- | --- |
| 面对模糊问题 | 谁在什么场景遇到什么困难？期望结果是什么？ | 一段具体的问题描述 |
| 拆解分析 | 输入、输出、操作步骤、限制和异常分别是什么？ | 子任务、边界条件、验收标准 |
| 找到根因 | 哪些因素阻碍目标？现有证据支持哪些原因？ | 已验证的原因或待验证假设 |
| 对比方案 | 有哪些可行办法？成本、风险和适用范围如何？ | 方案比较与选择理由 |
| 落地实现 | 最小可用版本是什么？先做哪一步？ | 能运行的代码和版本记录 |
| 验证与复盘 | 是否解决原问题？异常时表现如何？ | 验证记录、局限和后续改进 |

### 1.1 我的工作方式调整

| 工作环节 | 我的执行方法 |
| --- | --- |
| 开始实现 | 明确需求和验收标准，针对具体问题查资料 |
| 描述问题 | 说明目标、输入、预期输出以及已尝试的方法 |
| 功能验收 | 检查构建结果，验证正常操作、取消操作、错误输入和失败提示 |
| 故障定位 | 保留第一条有效错误，提出假设并逐个验证 |
| 确定优先级 | 优先完成读取、处理、保存流程，再按计划扩展功能 |
| 记录决策 | 说明技术选择理由、实现过程、验证证据和局限 |

### 1.2 我对项目问题的初步拆解

**模糊问题：**“图片太多，处理起来很麻烦。”

1. **拆解场景：**用户希望将同一文件夹中的多张图片，按同一组参数处理，结果保存到另一个文件夹。
2. **提出根因假设：**重复打开、调整和保存可能占用较多时间。我计划记录各步骤的耗时，确认哪些步骤适合批量执行。当前尚未完成测量。
3. **对比方案：**手动操作需要逐张执行；脚本可以统一处理文件，参数预览需要额外实现；桌面工具可以通过界面提供预览和结果反馈，同时需要开发和维护界面。结合本项目的交互需求，我选择桌面工具方案。
4. **我的选择：**开发带预览的桌面工具，复用同一套图像处理逻辑完成批量输出。
5. **最小落地：**先配置并验证开发环境，再完成单图打开、灰度化、恢复、保存，最后扩展文件夹处理。
6. **验收：**输入文件不被改写，输出符合参数要求，每个失败文件都有可理解的原因。

## 2. 项目目标与边界

### 2.1 产品目标

我计划开发一个 Windows 桌面工具，让用户可以预览处理效果，并将同一组图像处理参数应用到一批图片。

目标使用流程：

**打开样例图片 → 设置参数 → 预览效果 → 选择输入与输出目录 → 批量处理 → 查看结果。**

### 2.2 第一版功能范围

- 打开和预览 JPG、JPEG、PNG、BMP 图片。
- 灰度化、等比例缩放、亮度与对比度调整。
- 保留原图，支持恢复。
- 保存单张处理结果。
- 对输入目录中的图片应用同一组参数。
- 后台执行批量任务，显示进度、成功数、失败数和失败原因。
- 默认保护原文件，避免覆盖已有输出。
- 完成 Windows 打包验证和项目使用说明。

暂不纳入：AI、摄像头、登录、数据库、复杂插件体系、多级撤销、图层编辑。

### 2.3 初始边界决策

- 第一版批量处理仅扫描输入目录当前层，不递归子目录。
- 输入目录与输出目录必须不同；开始任务前生成固定文件清单。
- 遇到输出重名，生成带序号的新名称，不静默覆盖。
- 批量处理逐张进行，不一次加载整个文件夹的图片。
- 显示比例与实际像素缩放分开：预览放大不改变保存图片的尺寸。
- 参数变化从原图重新计算；预览与批量处理共用处理函数。
- 第一轮处理约定采用 8 位图片数据；透明通道的保留策略在图像转换阶段确认并记录。
- 输出格式、参数范围和处理顺序在对应功能开发前确定，不隐含在代码中。

## 3. 技术路线与环境验证

| 项目 | 初始选择 | 理由 |
| --- | --- | --- |
| 语言 | C++，以 C++17 为起点 | 满足项目所需能力，控制额外学习范围 |
| 界面 | Qt 6 Widgets | 适合桌面文件操作、参数控件和图片预览 |
| 图像处理 | OpenCV 4 | 实现灰度、缩放、亮度和对比度处理 |
| 开发工具 | Qt Creator | 集中完成编辑、构建和调试 |
| 构建 | CMake | 明确源码、依赖与构建配置 |
| 版本管理 | Git | 保存可运行阶段，便于对比与回退 |
| 目标平台 | Windows 64 位 | 先完成一个平台的完整交付 |

截至 2026-09-26，我已经安装 Qt 6.11.1 MSVC 2022 64 位组件，并在 Qt Creator 中使用对应 Kit 构建和运行 Qt Widgets 窗口。OpenCV 4.13.0 位于 `D:\Tools\OpenCV\opencv`，CMake 使用 `D:\Tools\OpenCV\opencv\build` 完成查找和链接配置。

当前使用 Qt MSVC 2022 64 位、MSVC x64 编译器和 OpenCV Windows 预编译库。这个组合已通过 CMake 配置和 Qt 窗口运行验证。OpenCV 函数调用、运行时 DLL 加载和图像读写仍需通过最小测试程序确认。

环境验收分两步，具体执行清单见第 6 节：

1. Qt 窗口能够构建和运行。
2. 在同一个 Qt 工程中调用 OpenCV，完成固定测试图片的读取、灰度转换和保存，并核对输出结果。此时不要求实现文件选择或图片预览界面。

Qt 窗口运行和 OpenCV 调用分别记录验收结果。第三方代码的参考记录包括来源、用途和本机验证状态。

## 4. 代码职责划分

起步阶段，我建立入口、主窗口和图像处理模块，后续根据功能需求增加文件。

```text
项目目录/
├── 开发文档.md
├── docs/环境配置.md
└── ImageBatchTool/
    ├── CMakeLists.txt         已创建：构建配置
    ├── main.cpp              已创建：程序入口
    ├── mainwindow.h/.cpp     已创建：主窗口骨架
    ├── mainwindow.ui         已创建：Qt Designer 界面文件
    └── imageprocessor.h/.cpp 后续创建：独立图像处理函数
```

后续增加：参数结构 ProcessOptions、后台任务 BatchWorker，以及必要的图像转换和文件读写辅助函数。

- MainWindow 负责界面与交互，ImageProcessor 负责图像处理并返回结果。
- 原始图片与当前处理结果分别保存，避免恢复功能依赖反向计算。
- Qt 图片与 cv::Mat 转换时，明确颜色通道、每行步长和内存所有权。
- 后台线程不直接修改界面，通过信号返回进度和结果。
- 读写失败和 OpenCV 异常应转换为可理解的错误信息。

## 5. 分阶段开发计划

我将 14 天作为第一轮开发目标，以阶段验收结果衡量进度。遇到阻塞时，我会记录原因、调整范围，不跳过基础流程。

| 阶段 | 计划时间 | 开发内容 | 完成标准 |
| --- | --- | --- | --- |
| A0 | 第 1 天 | 环境配置与最小验证 | Qt 工程构建运行成功，OpenCV 读取、转换和保存验证通过 |
| A1 | 第 2 天 | 打开和预览 | 支持正常图片、取消选择、损坏图片，显示比例正确 |
| B | 第 3～4 天 | 灰度、恢复、单图保存 | 完整单图流程可用，原文件保持不变 |
| C | 第 5～7 天 | 缩放、亮度、对比度、参数交互 | 参数有效且有边界检查，重复调整不累积误差 |
| D | 第 8～10 天 | 文件夹任务、后台执行、进度 | 窗口保持响应，任务逐张执行并统计结果 |
| E | 第 11～12 天 | 错误与边界处理 | 中文路径、重名、坏图、保存失败有明确处理 |
| F | 第 13～14 天 | 打包、说明和复盘 | 在没有开发环境的 Windows 环境验证运行 |

若进度不足，我优先减少处理效果种类，保留打开、处理、保存、批量反馈和原图保护。

## 6. 第一项开发任务：环境配置

### 6.1 本阶段目标

我先建立一套能够重复构建和运行的 Qt 6 + OpenCV 4 开发环境，再开始实现产品功能。本阶段通过最小验证工程检查编译器、Qt、OpenCV、CMake 和运行时依赖，并记录各环节的结果。

执行顺序：**盘点已有工具 → 确定匹配组合 → 补齐组件 → 配置 Qt Creator Kit → 验证 Qt → 接入并验证 OpenCV → 记录环境。**

### 6.2 配置任务清单

- [x] 确认已有 Qt、Qt Creator、CMake、构建工具、C++ 编译器和 Git 的版本及路径。
- [x] 检查 Visual Studio 的 C++ 工具链与 Windows SDK 组件，并通过实际构建确认可用性。
- [x] 确定本项目使用 MSVC 2022 x64，并选择匹配的 Qt 和 OpenCV 库。
- [x] 安装 Qt 6.11.1 MSVC 2022 64 位组件，保留原有 MinGW 环境。
- [x] 在 Qt Creator 中选择 MSVC 2022 64 位 Kit，并检查 Qt、编译器、CMake 和构建工具。
- [x] 创建 Qt Widgets + CMake 工程，构建并运行窗口。
- [x] 在同一工程中配置 OpenCV 的查找路径、头文件和链接依赖。
- [ ] 配置运行时库的查找方式，确认程序启动不缺少 Qt 或 OpenCV 动态库。
- [ ] 使用固定测试图片调用 OpenCV，完成读取、灰度转换、保存；确认输出文件可打开且结果正确。
- [ ] 从新的构建目录重新配置并构建，确认成功不依赖旧缓存。
- [ ] 记录实际版本、路径、配置步骤、错误与解决方法。

我先验证一种构建配置，并记录它是 Debug 还是 Release。另一种配置在需要时单独验证，分别记录结果。

### 6.3 环境记录表

| 组件 | 当前已知信息 | 待补充或验证 |
| --- | --- | --- |
| Qt | Qt 6.11.1 MSVC 2022 64 位，`D:\Qt\6.11.1\msvc2022_64` | 已完成窗口构建与运行验证 |
| C++ 编译器 | Visual Studio 2022 Community，MSVC x64 | 已通过实际构建验证 |
| Qt Creator Kit | Desktop Qt 6.11.1 MSVC2022 64bit | 调试器缺失不影响当前 Release 构建，后续调试前补充 |
| OpenCV | OpenCV 4.13.0，`D:\Tools\OpenCV\opencv` | CMake 查找与链接已通过；运行时调用待验证 |
| CMake 与构建工具 | Qt 自带 CMake，Ninja 生成器 | 已用于 Release 配置与构建 |
| Git | 已安装并在项目根目录初始化 | 已关联 GitHub 远端仓库 |
| 构建配置 | Release，MSVC 2022 x64 | Debug 暂不作为当前阶段验收项 |

### 6.4 环境验收标准

| 检查项 | 通过标准 | 当前结果 |
| --- | --- | --- |
| 工具链匹配 | Qt、OpenCV 与项目使用兼容的编译器和架构 | CMake 配置与链接通过，运行验证待完成 |
| Qt 构建运行 | CMake 配置、编译、链接成功，窗口正常打开 | 已通过 |
| OpenCV 接入 | 同一 Qt 工程可实际调用 OpenCV，启动时无缺库错误 | 查找与链接已通过，实际调用待验证 |
| 图像处理验证 | 固定测试图片读取成功，灰度结果保存且可打开 | 待验证 |
| 可重复构建 | 新构建目录按记录的配置可以重新构建运行 | 待验证 |
| 环境记录 | 版本、路径、配置方式和验证结果已记录 | 待完成 |

环境验收通过后，我再进入第二项任务。最小验证代码用于确认依赖和调用链可用；产品中的文件选择、处理交互和保存操作按后续任务单独实现并验收。

## 7. 第二项开发任务：打开并显示图片

前置条件：第 6 节的环境配置与验收已完成。

### 7.1 需求描述

用户点击“打开图片”，选择一个图片文件，窗口按比例显示它。取消选择时不改变现有内容；读取失败时提示错误并保留当前有效图片。

### 7.2 实现前需要明确的设计问题

1. 使用什么显示控件？QLabel 与 QGraphicsView 各有什么成本？
2. 图片数据保存在哪里？界面显示内容和原始像素是否分离？
3. 窗口尺寸变化时，如何重新计算显示比例？
4. 文件选择成功是否代表图片读取成功？失败时如何保留旧状态？

我的初步方案是采用 QLabel 配合布局显示图片，以较低复杂度完成等比例预览。后续需要平移、复杂缩放或图形交互时，我再评估 QGraphicsView。当前选择仍需通过实际实现与验收确认。

### 7.3 任务拆解

- [ ] 基于已通过环境验收的 Qt Widgets 工程开始实现。
- [ ] 添加打开按钮和图片显示区域。
- [ ] 连接按钮与文件选择逻辑。
- [ ] 将文件读取到临时图片对象，检查成功后再替换当前状态。
- [ ] 实现等比例显示和窗口尺寸变化后的更新。
- [ ] 处理取消操作和读取失败。
- [ ] 按下表手动验收，记录结果。
- [ ] 保存一个可运行的 Git 提交。

### 7.4 验收清单

| 场景 | 预期行为 | 当前结果 |
| --- | --- | --- |
| 打开横图、竖图 | 无比例失真 | 待验证 |
| 连续打开两张图片 | 正确显示第二张 | 待验证 |
| 取消文件选择 | 原内容不变 | 待验证 |
| 打开损坏图片 | 提示失败，保留原内容，不崩溃 | 待验证 |
| 调整窗口大小 | 图片保持合理显示比例 | 待验证 |
| 文件名或目录包含中文 | 可以正常打开 | 待验证 |

## 8. 我的排错与方案决策方法

遇到问题时，我先记录目标行为、实际现象、复现步骤、第一条有效错误和已经尝试的方法，再根据证据提出原因假设。每次尽量只改变一个关键变量，确认是哪项修改产生了效果。

我对代码实现、技术选择和验证结果负责。查阅文档、参考开源代码或寻求外部帮助后，我会先理解方案的适用条件，再决定是否纳入工程，并亲自验证结果。对于重要决策，我记录候选方案、取舍理由和已知局限。

### 8.1 问题记录模板

```text
日期：YYYY-MM-DD
问题：
预期行为：
实际行为与复现步骤：
已有证据（错误信息、日志、输入样例）：
原因假设：
验证方法与结果：
确认的根因 / 尚未确定的部分：
候选方案与取舍：
最终实现：
验收结果：
关联提交：
```

### 8.2 我判断功能完成的标准

- 能说明该功能解决的问题和不覆盖的范围。
- 正常流程与关键异常情况经过验证。
- 失败有提示，不损坏原文件或现有有效状态。
- 能解释主要代码和方案选择。
- 留下版本记录；必要时同步更新文档。

## 9. 交付与复盘

我计划交付源码、准确的构建说明、使用说明、可运行发布包、验证记录和来源说明。复用第三方代码时，我会保留对应来源和许可证文件。

我在简历中只描述实际完成且能够解释的部分。若记录性能数据，我会同时记录测试环境、图片数量、图片尺寸和测量方法，确保结果可追溯。

复盘时，我会回顾：哪个需求最初不明确？哪个故障通过证据定位？比较过哪些方案？哪些设计后来调整？哪些功能我可以独立重新实现？

## 10. 开发日志

我按 factual-writing-style 技能记录开发过程：区分已确认事实、待验证假设和计划；结论附带依据与适用范围；出现错误时记录具体修正和验证结果。技术方案比较围绕成本、依赖和功能条件展开。

### 2026-09-23

- 建立独立项目目录与初版开发文档。
- 明确以独立完成需求分析、实现、验证和交付为训练目标。
- 确定“模糊问题 → 拆解分析 → 根因 → 方案对比 → 实现 → 验证复盘”的工作流程。
- 确定 Qt 6 + OpenCV 4 批量图像处理工具方向，旧 ImageEditor 仅作参考。
- 第一项任务确定为“环境配置”，验收通过后再实现“打开并显示图片”。
- 以开发者第一人称记录目标、计划、技术决策和复盘方法。
- 补充环境配置清单、环境记录表和验收标准。
- 按 factual-writing-style 技能调整表述，将本次修订版本设为 0.1；后续修订按 0.2、0.3 等版本号递增。
- 待办：盘点已有工具，确定依赖组合，完成环境配置与最小验证。

### 2026-09-26

- 安装并选择 Qt 6.11.1 MSVC 2022 64 位 Kit，Qt Widgets 窗口构建和运行成功。
- 确定 OpenCV 4.13.0 安装目录为 `D:\Tools\OpenCV\opencv`。
- 在 CMake 中加入 OpenCV 的查找、头文件和链接配置，Release 配置成功。
- 清理 Qt Creator 中重复的 CMake 参数，恢复正常配置。
- 初始化 Git 仓库并关联 `https://github.com/GreenLoong/pixel-workshop.git`。
- 将文档版本更新为 0.2。下一项验收是实际调用 OpenCV，完成灰度转换、保存和重新读取。
